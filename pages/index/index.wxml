<!--index.wxml-->
<view >
      <swiper class="banner"
        indicator-dots="true"
        autoplay="true"
        interval="3000"
        duration="1000"
      >
        <block wx:for="{{imgUrls}}">
          <swiper-item>
            <image src="{{item}}" class="slide-image" background-size="cover" width="355" height="150" />
          </swiper-item>
        </block>
      </swiper>
</view>

<!-- 
<button type='primary' bindtap='request'>请求</button>
<view wx:for='{{lati}}' wx:for-index='ind' wx:for-item='im'>
    {{ind}}:{{im.id}}-{{im.name}}-{{im.password}}
  </view> -->


